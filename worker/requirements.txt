# Pinned to a recent release, not a fixed old one — YouTube changes its
# player/signature/format serving often enough that a stale yt-dlp silently
# stops matching real formats (symptom: "Requested format is not available").
# Bump this periodically; check https://github.com/yt-dlp/yt-dlp/releases.
yt-dlp==2026.07.04
PySocks==1.7.1
redis==5.2.0
rq==2.10.0
boto3==1.35.63
supabase==2.9.1
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.1
